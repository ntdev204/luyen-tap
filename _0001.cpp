// Tính chu vi, diện tích hình vuông
#include <iostream>

using namespace std;

int main() {
    int a, cv, dt;
    cin >> a;
    cv = a * 4;
    dt = a * a;
    cout << cv << " " << dt;
    return 0;
}